react-dom: Used to reflect the changes to the dom that is being loaded when webpage is 

webpack: Used to bundle the files.

webpack-cli: USed to speed up while setting up webpack project

webpack-dev-server: Gives a commands to start the server with hot-reloading 

@babel dependencies : USed to transpile the code into browser's understandable code from es2015 to plain js


babel-loader: Used to process the different file types - this is used to transpile js

sass-loader : USed to transpile sass files to css

style-loader : used to add styles in the style tag of the build 

@babel/preset-react: Have presets to all react plugins, transpile the jsx into the plain html

@babel/preset-env: Allows to use latest es16 js without worring about the browsers support